# Run memory — 2026-08-26 21:30 — full-suite

## Run metadata

| Field    | Value                                                                                 |
| -------- | ------------------------------------------------------------------------------------- |
| Date     | 2026-08-26 21:30 → 2026-08-27 (UTC)                                                   |
| Agent    | Devin (automation session, scheduled daily run)                                       |
| Trigger  | scheduled                                                                             |
| Command  | `npx playwright test` (executed as 15 chunks, each preceded by `specs/auth.setup.ts`) |
| Base URL | cog-enterprise-qa.beta.devinenterprise.com                                            |
| Scope    | all specs (`setup`, `unauthenticated`, `authenticated`, `member`)                     |

As on 2026-08-24, a single process cannot finish the suite: the shared admin storage state
expires mid-run and later specs redirect to `/auth/login`. The run was executed as 15
sequential `--no-deps` chunks, re-authenticating before each chunk, and the blob reports were
merged into one canonical report (366 unique tests, no duplicates).

## Results

| Total | Passed | Failed | Flaky | Skipped |
| ----- | ------ | ------ | ----- | ------- |
| 366   | 317    | 46     | 0     | 3       |

Skipped: `ENV-REG04`, `ENV-REG05` (unmet preconditions) and the member auth setup step (no
member credential — INFRA-1).

Artifacts (local, not committed): `/home/ubuntu/qa-artifacts-2026-08-26/` — `merged.json`,
`failures.txt`, `blob/c01..c15.zip`, `run.log`, per-failure `test-results-c*/` (screenshots +
videos), fix verification runs under `test-results-fix1..fix9/` and `logs/fix*.log`, live DOM
probes under `probe/`.

## Failure triage

Classification key: **script** = locator drift / stale assumption (fixed in this PR),
**app** = product behavior defect (reported only, test left failing), **infra** = environment,
credential, or fixture gap, **inconc** = inconclusive.

| Test ID       | Error (short)                                                       | Classification | Evidence                                                                                             | Action                                        |
| ------------- | ------------------------------------------------------------------- | -------------- | ---------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| ORGCRT-REG02  | `spinbutton "Monthly ACU limit"` resolved 0 elements                | script         | live DOM: `input[type=text][inputmode=numeric][name=maxAcuLimit]`, a11y role textbox                 | page-object locator → `getByRole` textbox     |
| ORGCRT-REG07  | timeout on the same stale ACU locator                               | script         | as above                                                                                             | fixed with ORGCRT-REG02                       |
| ORGCRT-REG10  | timeout on the same stale ACU locator                               | script         | as above                                                                                             | fixed with ORGCRT-REG02                       |
| ORGCRT-E2E01  | timeout on the same stale ACU locator                               | script         | as above                                                                                             | fixed with ORGCRT-REG02                       |
| ORG-REG07     | expected ACU field to reject text (`""`), got `"abc"`               | script         | live DOM keeps `abc` with `aria-invalid="true"` and Save disabled                                    | assertion updated to the real contract        |
| ENTSET-SAN02  | test timeout waiting for `xfri-5` link in the sidebar               | script         | sidebar Organizations section has its own Search toggle → `Search organizations` filter reveals it   | page object filters instead of paginating     |
| STOOL-SMK01   | `tablist` button `Progress` resolved 0 elements                     | script         | session opens with no tool tab; the empty panel lists launchers, `Add tab` offers the same inventory | launcher opened explicitly, then tab asserted |
| SCHED-REG01   | `main input[type=datetime-local]` resolved 0 elements               | script         | Run once trigger renders an `at: …` popover button + date/time dialog with Start date/Start time     | new `setRunOnceDateTime()` picker helper      |
| SCHED-E2E01   | same stale datetime input; after fix, no `Succeeded` event in 7 min | script + app   | scheduling now works; the scheduled run never completes (APP-5)                                      | script fixed, assertion left failing          |
| AUTO-E2E01    | webhook run: no `Succeeded` event in 3 min                          | app            | APP-5                                                                                                | reported, left failing                        |
| SUB-AU-E2E01  | schedule run: no `Succeeded` event in 3 min                         | app            | APP-5                                                                                                | reported, left failing                        |
| ECON-E2E01    | session never printed `MCP-CONTEXT7-AVAILABLE` (300 s)              | inconc         | agent-session dependent; session stayed in setup                                                     | needs a longer window / real backend          |
| KNOW-E2E01    | session never printed `KNOWLEDGE-TRIGGER-CONFIRMED` (180 s)         | inconc         | as above                                                                                             | as above                                      |
| PLAY-E2E01    | session body did not contain the playbook name                      | inconc         | session was still "Devin is starting up…" when asserted                                              | as above                                      |
| GEN-REG01     | Require SSO toggle: expected `true` after reload, got `false`       | app            | APP-3, screenshot `test-results-c*/…GEN-REG01…`                                                      | reported, left failing                        |
| PREF-REG03    | Newsletter: expected flipped `false` after reload, got `true`       | app            | APP-4; single-toggle probe persists, batch flip loses Newsletter                                     | reported, left failing                        |
| ENTSET-REG03  | browser Back after "Back to app" lands on `/org-selector`           | app            | APP-6, `test-results-fix4/…and-return-via-browser-Back…`                                             | reported, left failing                        |
| ENV-REG03     | `heading "Outposts"` absent                                         | app            | APP-7 (page renders no Outposts section)                                                             | reported, left failing                        |
| COMP-REG05    | Send stayed enabled for a 9,976-char prompt                         | app            | APP-8                                                                                                | reported, left failing                        |
| SUB-OS-SMK01  | console `401` on cold load                                          | app            | APP-1 (repeat of 2026-08-24)                                                                         | reported, left failing                        |
| SUB-OS-SAN01  | console `401`                                                       | app            | APP-1                                                                                                | reported                                      |
| SUB-OS-REG01  | console `401`                                                       | app            | APP-1                                                                                                | reported                                      |
| SUB-OS-REG02  | console `401`                                                       | app            | APP-1                                                                                                | reported                                      |
| SUB-OS-REG03  | console `401`                                                       | app            | APP-1                                                                                                | reported                                      |
| SECRET-SMK01  | console `401` on Secrets cold load                                  | app            | APP-1                                                                                                | reported                                      |
| SCAN-SMK01    | console `401` on Security cold load                                 | app            | APP-1                                                                                                | reported                                      |
| ROLE-SAN02    | console `502` opening the scope filter                              | app            | APP-9                                                                                                | reported                                      |
| PCON-SMK01    | `#personal-slack-link` / `#personal-github-link` absent             | infra          | personal Connections renders only providers connected at enterprise level (Slack "Not connected")    | tenant connection state — not patched         |
| PCON-SAN01    | same                                                                | infra          | as above                                                                                             | as above                                      |
| PCON-REG01    | same                                                                | infra          | as above                                                                                             | as above                                      |
| AUTH-REG01    | OTP code field never rendered                                       | infra          | error context alert: "Rate limit exceeded. Please try again later."                                  | needs a cooled-down login/OTP quota           |
| AUTH-REG02    | OTP code field never rendered                                       | infra          | same rate-limit alert                                                                                | as above                                      |
| AUTH-REG05    | OTP code field never rendered                                       | infra          | same rate-limit alert                                                                                | as above                                      |
| AUTH-SAN04    | no resend control on the verification step                          | app            | APP-12 (repeat of 2026-08-24; no rate-limit alert in this context)                                   | reported, left failing                        |
| MEMB-E2E01    | Gmail OTP timed out (invite acceptance)                             | infra          | INFRA-2 (shared inbox / OTP quota)                                                                   | reported                                      |
| REV-E2E01     | GitLab `GET /merge_requests` → 401                                  | infra          | INFRA-3 (`GITLAB_QA_API_TOKEN` rejected)                                                             | reported                                      |
| REPO-REG01    | QA repo row absent from the permissions table                       | infra          | INFRA-4 (repo fixture not granted to this enterprise)                                                | reported                                      |
| REPO-REG03    | timeout adding a permission for the same repo                       | infra          | INFRA-4                                                                                              | reported                                      |
| REPO-E2E01    | repo list did not contain the QA project                            | infra          | INFRA-4                                                                                              | reported                                      |
| MBR-REG01..07 | `ENOENT .auth/member.json`                                          | infra          | INFRA-1 (no member credential configured)                                                            | reported (7 tests)                            |

### Script fixes verified

All fixed specs were re-run with `--repeat-each=3` (logs under
`/home/ubuntu/qa-artifacts-2026-08-26/logs/fix*.log`):

| Spec                              | Result                                                           |
| --------------------------------- | ---------------------------------------------------------------- |
| `org-create.spec.ts` (whole file) | 30/30 passed                                                     |
| `e-orgs.spec.ts` (ORG-REG07)      | 3/3 passed                                                       |
| `ent.spec.ts` (whole file)        | ENTSET-SAN02 + ENTSET-REG04 3/3 passed; ENTSET-REG03 red (APP-6) |
| `session.spec.ts` (STOOL-SMK01)   | 3/3 passed                                                       |
| `automations.spec.ts` SCHED-REG01 | 3/3 passed                                                       |
| `automations.spec.ts` SCHED-E2E01 | 0/3 — scheduling works, run never succeeds (APP-5)               |

Old → new locators:

| Element                     | Old                                 | New                                                                         |
| --------------------------- | ----------------------------------- | --------------------------------------------------------------------------- |
| Monthly ACU limit (create)  | `getByRole("spinbutton", …)`        | `getByRole("textbox", { name: "Monthly ACU limit" })`                       |
| Run once date/time          | `main input[type="datetime-local"]` | `main` → `getByRole("button", { name: /^at: / })` + date/time dialog helper |
| Session tool tab (initial)  | `tablist` → button `Progress`       | launcher `getByRole("button", { name: /^Progress\b/ })`, then the tab       |
| Sidebar organization lookup | repeated `Load more` clicks         | Organizations section `Search` toggle → `Search organizations` filter       |

## App issues (reported, not patched)

**APP-1 — 401s on cold load of sub-org Settings / Secrets / Security** (repeat)
Tests: `SUB-OS-SMK01/SAN01/REG01/REG02/REG03`, `SECRET-SMK01`, `SCAN-SMK01` — assertion:
console errors array is empty.
Repro: log in as enterprise admin → open `/org/jeet-devin-qa/settings` (or `…/settings/secrets`,
`/settings/security`) with a clean cache while watching the console.
Expected: no failed requests. Actual: 2–4× `Failed to load resource: … status of 401`.

**APP-3 — "Require SSO" does not persist**
Test: `GEN-REG01` — `expect(ssoSwitch).toHaveAttribute("aria-checked", toggled)` after reload.
Repro: `/org/cog-enterprise-qa/settings/general` → toggle Require SSO → reload.
Expected: the new state survives the reload. Actual: switch is back to `false`.

**APP-4 — Newsletter preference lost when several toggles are flipped in one visit**
Test: `PREF-REG03` — flipped state after reload.
Repro: `/settings/preferences` → flip Play sound, In-app notifications, Newsletter,
Auto-approve child sessions, Auto-approve workflows → reload.
Expected: every flip persists. Actual: Newsletter is back to its previous value (`true`); a
single Newsletter flip in isolation does persist.

**APP-5 — Automation runs never reach "Succeeded"**
Tests: `AUTO-E2E01` (webhook), `SUB-AU-E2E01` (manual schedule run), `SCHED-E2E01` (run-once
schedule) — `expect(succeededEventLinks.first()).toBeVisible()`.
Repro: create an automation in `jeet-devin-qa`, trigger it (webhook POST with the correct
secret returns `200 {"status":"accepted"}`, or wait for a run-once schedule), poll the
automation's events list for up to 7 minutes.
Expected: an event linking to `/sessions/<id>` with status Succeeded. Actual: no Succeeded
event appears; the spawned session stays in "Devin is starting up…"/setup.

**APP-6 — Browser Back after "Back to app" lands on the org selector**
Test: `ENTSET-REG03` — `expect(entSettingsHeading).toBeVisible()`.
Repro: `/org/cog-enterprise-qa/settings` → click "Back to app" → browser Back.
Expected: return to Enterprise Settings. Actual: `/org/cog-enterprise-qa/org-selector`.

**APP-7 — Environment page renders no Outposts section**
Test: `ENV-REG03` — `expect(heading "Outposts").toBeVisible()`.
Repro: `/org/cog-enterprise-qa/settings/environment`.
Expected: an Outposts section (configuration/list/empty state). Actual: heading absent.

**APP-8 — Send stays enabled for an over-limit prompt**
Test: `COMP-REG05` — `expect(sendButton).toBeDisabled()`.
Repro: paste a 9,976-character prompt with Unicode/script content into the composer.
Expected: Send disabled with a length error. Actual: Send enabled.

**APP-9 — 502 opening the roles scope filter**
Test: `ROLE-SAN02` — console errors empty.
Repro: `/org/cog-enterprise-qa/settings/roles` → open the scope filter.
Actual: `Failed to load resource: … status of 502`.

**APP-12 — No resend control on the OTP verification step** (repeat)
Test: `AUTH-SAN04`.
Repro: log in with email → reach the verification-code step.
Expected: a "Resend"/"Send a new code" control. Actual: none.

## Infrastructure gaps

- **INFRA-1** — no member credential ⇒ `.auth/member.json` missing, 7 `MBR-*` tests cannot run.
- **INFRA-2** — Gmail OTP quota/inbox contention: `MEMB-E2E01` and the `AUTH-REG0*` login tests
  hit "Rate limit exceeded" (aggravated by 15 re-authentications per suite run).
- **INFRA-3** — `GITLAB_QA_API_TOKEN` rejected with 401 ⇒ `REV-E2E01`.
- **INFRA-4** — the QA repository fixture is not granted to this enterprise ⇒ `REPO-REG01`,
  `REPO-REG03`, `REPO-E2E01`.
- **INFRA-5** — Slack/GitHub are not connected at the enterprise level, so the personal
  Connections page renders neither row ⇒ `PCON-SMK01/SAN01/REG01`. Tenant state, not test drift;
  assertions left intact.
