# Run memory — 2026-09-02 21:33 — full-suite

## Run metadata

| Field    | Value                                                                              |
| -------- | ---------------------------------------------------------------------------------- |
| Date     | 2026-09-02 21:33 (UTC)                                                             |
| Agent    | Devin (scheduled daily Playwright run)                                             |
| Trigger  | scheduled                                                                          |
| Command  | `npx playwright test specs/auth.setup.ts`, then `npx playwright test` (full suite) |
| Base URL | cog-enterprise-qa.beta.devinenterprise.com                                         |
| Scope    | all specs (`setup`, `unauthenticated`, `authenticated`, `member`)                  |

## Results

| Total | Passed | Failed | Flaky | Skipped |
| ----- | ------ | ------ | ----- | ------- |
| 366   | 320    | 43     | 0     | 3       |

Admin auth setup passed (member setup skipped — no member email configured). Full-run log:
`/home/ubuntu/qa-artifacts-2026-09-02/full-run.log`; JSON report `test-results/results.json`; HTML
report `playwright-report/`. Re-run logs: `rerun1.log` (7 repaired/suspect specs,
`--repeat-each=3`), `rerun2.log` (ECON-MCP-REG02 after the page-object fix, `--repeat-each=3`).

Pre-run check: `jeet-devin-qa` Monthly ACU limit read `No limit` (the 2026-08-31 cascade did not
recur; COMP-\*, PLAY-E2E01, ORG-REG07/08/11 all passed this run).

## Failure triage

| Test ID                                                | Error (short)                                                                      | Classification               | Evidence                                                                               | Action                                                        |
| ------------------------------------------------------ | ---------------------------------------------------------------------------------- | ---------------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| KNOW-SAN03, MEMB-SMK01, ORGSEL-REG10, SUP-REG01        | docs link/popup now on `docs.devinenterprise.com`, specs asserted `docs.devin.ai`  | script — outdated assumption | `full-run.log`; live href `https://docs.devinenterprise.com/enterprise`                | fixed — shared `DOCS_URL_PATTERN` in `support/paths.ts`       |
| AUTO-REG04                                             | option `Roast Commits` never appears after typing `!roast`                         | script — locator drift       | probe: macro menu options are `!roaster_hulk`, `!roast`; chosen chip reads the title   | fixed — select the `!roast` option, keep the chip assertion   |
| ECON-MCP-REG02                                         | timeout after switch click; modal `Hide Airtable from the marketplace?`            | script — bad precondition    | `test-results/…e-conn…/test-failed-1.png`: Airtable now has 1 org installation         | fixed — page object picks a server with 0 orgs/users/sessions |
| PREF-REG04                                             | expected `Devin Review`, got `In-session` for "Pull request links open"            | flaky / state-dependent      | passes 3/3 unchanged in `rerun1.log`                                                   | none — watch; likely cross-worker interference on prefs       |
| AUTH-REG02, AUTH-REG05                                 | OTP field never appears; `alert: Rate limit exceeded`                              | infrastructure               | `test-results/authenticated-auth-*/error-context.md`                                   | none — login OTP quota                                        |
| MEMB-E2E01                                             | Gmail OTP/invite mail timeout                                                      | infrastructure               | `full-run.log`                                                                         | none — shared inbox                                           |
| REV-E2E01                                              | `GitLab GET /merge_requests?per_page=1 failed: 401`                                | infrastructure               | `full-run.log`                                                                         | reported — `GITLAB_QA_API_TOKEN` rejected (recurring)         |
| MBR-REG01…REG07                                        | `ENOENT .auth/member.json`                                                         | infrastructure               | `test-results/member-authz-*/`                                                         | reported — no member credential configured (recurring)        |
| SKILL-SMK01/SAN01/SAN02/REG01/REG02/REG03/REG05/REG06  | Skills heading/table absent; page timeouts                                         | app issue — feature removed  | sweep: `/settings/enterprise-skills` renders 404 (HTTP 200); `sweep/skr-route.png`     | reported (APP-15, recurring since 2026-08-31)                 |
| ENTSET-SAN05, ENTSET-REG01, SUB-OS-SAN01, SUB-OS-REG01 | `Skills & Rules` link absent from hub / sub-org root                               | app issue — same removal     | sweep: hub links …Learning, Usage Policies, Devin Coach; sub-org root has no Skills    | reported (APP-15)                                             |
| ENV-REG03                                              | `Outposts` heading absent                                                          | app issue                    | sweep: tabs Rollout / Configuration / Blueprint; `?tab=outposts` falls back to Rollout | reported (recurring since 2026-08-18)                         |
| PLAY-REG12                                             | session page never shows playbook name; `Setting up…`, 403 on `/api/vscode-server` | app issue                    | `test-results/authenticated-e-playbooks-*/test-failed-1.png`                           | reported — session launched but the playbook chip is missing  |
| KNOW-E2E01                                             | Usage never lists the triggered session (120 s poll)                               | app issue                    | `full-run.log`                                                                         | reported (recurring)                                          |
| STOOL-SMK01                                            | no `tabpanel` role for the Progress tab                                            | app issue                    | `test-results/authenticated-session-*/`                                                | reported (APP-13, recurring since 2026-08-27)                 |
| PCON-SMK01, PCON-SAN01, PCON-REG01                     | personal Slack/GitHub rows absent (GitLab, self-hosted GitLab, Linear only)        | app/environment (config)     | `test-results/authenticated-pconn-*/`                                                  | reported (recurring)                                          |
| REPO-REG01, REPO-REG03, REPO-E2E01                     | permission row missing; drawer timeout; composer repo list empty                   | app/environment              | `test-results/authenticated-e-repos-*/`; sweep `repo-cold.png` (no org preselected)    | reported — QA repo fixture/provider unavailable (recurring)   |

### App issues (bug reports)

**APP-15 — Skills & Rules page removed (SKILL-\*, ENTSET-SAN05/REG01, SUB-OS-SAN01/REG01)**

1. Failing assertion: `SkillsPage.heading` visible / `Skills & Rules` link in settings hub.
2. Repro: log in as enterprise admin → `/org/cog-enterprise-qa/settings` → look for
   `Skills & Rules`; or open `/org/cog-enterprise-qa/settings/enterprise-skills`.
3. Expected: Skills & Rules entry in the Resources group and an analytics page at the route.
4. Actual: no entry (hub lists Learning, Usage Policies, Devin Coach instead); route renders a
   "404 This page could not be found" body with HTTP 200. Unconfirmed whether the removal is
   intentional — specs left failing until product confirms.

**Environment has no Outposts tab (ENV-REG03)**

1. Assertion: `Outposts` heading visible.
2. Repro: `/org/cog-enterprise-qa/settings/enterprise-environment?tab=outposts`.
3. Expected: Outposts tab (or an explicit "unknown tab" state).
4. Actual: tabs are Rollout / Configuration / Blueprint; unknown `?tab=` silently selects Rollout.

**Playbook-launched session never shows the playbook (PLAY-REG12)**

1. Assertion: session main panel contains the `qa-temp-*` playbook name.
2. Repro: Playbooks → create disposable playbook → Run/launch a session in `jeet-devin-qa`.
3. Expected: the new session references the playbook in the composer/main panel.
4. Actual: session page stays at "Setting up… / In progress" with no playbook chip; console shows
   HTTP 403 from `/api/vscode-server/...`.

**Knowledge Usage never lists a triggered session (KNOW-E2E01)** — recurring; same steps as prior
runs: trigger a session that recalls `backend based code`, open the entry's Usage tab; expected the
session row within 120 s; actual none.

**Progress tool panel lacks `role="tabpanel"` (STOOL-SMK01)** — recurring APP-13.

**Personal connections missing Slack/GitHub rows (PCON-\*)** and **repository fixture unavailable
(REPO-\*)** — environment configuration; reported as before, not patched around.

## Actions taken

- Script fixes (branch `fix/playwright-docs-host-macro-mcp-2026-09-02`):
  - `support/paths.ts` — new `DOCS_URL_PATTERN` accepting `docs.devin.ai` and
    `docs.devinenterprise.com`; used by KNOW-SAN03, MEMB-SMK01 (path `/enterprise` still asserted),
    ORGSEL-REG10, SUP-REG01.
  - `automations.spec.ts` — AUTO-REG04 selects the `!roast` option (exact) and still asserts the
    `Roast Commits` chip.
  - `pages/connections.page.ts` — `unusedMcpServerName()` picks the first MCP row with 0
    organizations / users / sessions (name from the row link; the avatar initials are
    `aria-hidden`); `ECON-MCP-REG02` uses it instead of the hard-coded `Airtable`, which now has an
    organization installation and prompts a destructive "Hide from marketplace" confirm.
  - All seven fixed/suspect specs pass `--repeat-each=3` (`rerun1.log` 18/18 excluding
    ECON-MCP-REG02; `rerun2.log` 3/3 for ECON-MCP-REG02).
- Pre-existing `prettier --check` warnings on `memory/2026-08-14-2224-full-suite.md` and
  `memory/2026-08-19-2130-full-suite.md` are on `main` and untouched.
- No app or catalog changes. No tests skipped or weakened.

## Enterprise sweep (cognition-e2e-platform skills, browser)

Method: coverage diff of the 22 `devin-enterprise-web-settings-*` skills (263 TCs,
`/home/ubuntu/qa-artifacts-2026-09-02/enterprise-tcs.txt`) against `catalog/pages/*.json` (336
cases) and the 361 Playwright titles. Every skill area has a spec file (`e-*.spec.ts`,
`ent.spec.ts`, `roles.spec.ts`, `groups-idp.spec.ts`, `support.spec.ts`, …) and the IDOR /
injection / save-failure / deep-link cases are covered by the `*-REG*` specs, so the browser pass was
limited to cases whose Playwright equivalent failed this run plus exploration. Executed with the
admin storage state in a headless Chromium on the enterprise domain only (no OTP consumed);
screenshots in `/home/ubuntu/qa-artifacts-2026-09-02/sweep/`, log `sweep.log` / `sweep2.log`.
Read-only except typed drafts that were never submitted (`qa-temp-*`, verified absent afterwards).

Skipped as already covered and passed by Playwright: hub (TC-HUB except the Skills entry), general,
connections incl. MCP (TC-CONN-011/013 pass after the fix), sessions, Devin, review, knowledge
(except Usage E2E), playbooks (except PLAY-REG12), environment TC-ENV-001…018 (ENV-REG10/REG11 cover
016–018 and passed), secrets, security, guardrails, analytics, infrastructure, support, Devin API,
membership groups/roles/members, organizations, sub-org root (except Skills link).

| Case                                                              | Verdict    | Notes / evidence                                                                                                                                         |
| ----------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TC-SKR-001/002 — Skills & Rules loads, breadcrumb                 | FAIL (app) | Route renders in-app 404 (HTTP 200); no hub or sub-org link. `sweep/skr-route.png`, `sweep/hub.png`                                                      |
| TC-SKR-003…008                                                    | NOT TESTED | Blocked by the above                                                                                                                                     |
| TC-ENV-007 — tabs inventory                                       | FAIL (app) | Rollout / Configuration / Blueprint only; no Outposts / Golden-snapshot; `?tab=outposts` → Rollout. `sweep/env-tabs.png`, `env-outposts-deeplink.png`    |
| TC-ENV-016/017/018 — editor guard, YAML validation, dirty/discard | PASS\*     | \*Executed by ENV-REG10 / ENV-REG11 (passed). Not re-run in the browser                                                                                  |
| TC-ENV-019 — caret & keyboard navigation                          | PASS\*     | \*Ctrl+Home caret path executed inside ENV-REG11 (passed); click-to-caret not separately exercised                                                       |
| TC-REPO-005/006 — search, Manage permissions                      | INCONCL.   | Page loads with "Select an organization"; no search or Manage button until an org is chosen; org fixture unavailable (REPO-\* failures). `repo-cold.png` |
| `/settings/learning` (replacement surface)                        | PASS       | 3 stat cards, member table (64 members), org filter, hide-inactive. `sweep/learning.png`                                                                 |
| Personal connections / Progress tabpanel                          | NOT TESTED | Playwright failure stands as evidence                                                                                                                    |
| Non-admin authorization (TC-\*-008 family, TC-HUB-008)            | NOT TESTED | No member credential                                                                                                                                     |

### Exploratory pass (`devin-enterprise-web-exploration` + `qa-interaction-matrix`)

| Charter / cell                                                     | Verdict  | Finding                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Secrets — Bulk add panel, hostile paste, dirty → Back/Forward      | PASS     | `qa-temp-dirty-<script>x</script>` keeps `Store` disabled (format validation); Back leaves the SPA (panel is not a history entry), Forward returns with the panel closed and draft gone; nothing created. `sec-panel-dirty.png`, `sec-after-forward.png` |
| Playbooks — create page, name typed, in-app nav to Knowledge, Back | FAIL     | No unsaved-changes guard: navigating away drops the draft silently; Back returns to `/playbooks/create` empty. Save is disabled with name only (OK). `pb-create-dirty.png`, `pb-after-back.png`                                                          |
| Roles — Create for enterprise form, whitespace name, 300-char name | PASS/⚠  | Whitespace-only name keeps submit disabled; a 308-char name is accepted client-side with no max length (server not exercised — never submitted). `role-form-hostile.png`                                                                                 |
| Hub — bogus `/settings/does-not-exist`                             | PASS     | Sidebar + in-app 404 body, HTTP 200 (soft 404, consistent with the app)                                                                                                                                                                                  |
| Membership — duplicate/hostile `?tab=` param                       | PASS     | Falls back to Members; no script execution                                                                                                                                                                                                               |
| Hub — Back/Forward across settings → general → analytics           | PASS     | History entries correct in both directions                                                                                                                                                                                                               |
| Sub-org — bogus slug `/org/nonexistent-org-qa-temp/settings`       | PASS     | 404 body, no data leak                                                                                                                                                                                                                                   |
| Sub-org settings root links                                        | PASS     | General, Connections, Devin, DeepWiki, Schedules, Knowledge, Environment, Playbooks, Secrets, Repositories, Membership, Devin API, Analytics, Devin Coach — no Skills & Rules                                                                            |
| Sub-org landing 4xx/5xx watch                                      | INCONCL. | Page never reached network-idle (long polling); not evaluated                                                                                                                                                                                            |

**Finding EXP-PB-01 — Playbook create draft lost without warning.** Repro: Enterprise → Playbooks →
Create playbook → type a name → click Knowledge in the sidebar. Expected: unsaved-changes prompt (or
draft retained on Back). Actual: navigates immediately; Back shows an empty create form. Same
defect class as the blueprint editor loss reported 2026-08-31 (TC-ENV-016 then FAIL; this run
ENV-REG10 passed, so the blueprint guard may have been fixed — the playbook form has not).

**Observation EXP-ROLE-01 — no client-side max length on role name** (308 chars accepted, not
submitted). Low severity; server behaviour untested.

## Learnings for next run

- Enterprise docs links now point at `docs.devinenterprise.com`; use `DOCS_URL_PATTERN` rather than
  hard-coding a host.
- The automation `!macro` menu lists options by macro name (`!roast`), not playbook title.
- MCP table rows: the first cell contains an `aria-hidden` initials avatar plus the name link, so
  `textContent` reads `ADAlloyDB`; take the name from the row's link. Marketplace state changes —
  Airtable now has an org installation, so never hard-code a "disposable" server.
- PREF-REG04 failed once and then passed 3/3; if it repeats, suspect another worker's prefs test.
- `/settings/enterprise-skills` is a soft 404 (HTTP 200); `res.status()` cannot detect it.
- Playbook create form has no dirty guard; do not rely on Back to recover a draft.
