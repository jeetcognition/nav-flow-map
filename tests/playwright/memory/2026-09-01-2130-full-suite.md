# Run memory — 2026-09-01 21:30 — full-suite

## Run metadata

| Field    | Value                                                             |
| -------- | ----------------------------------------------------------------- |
| Date     | 2026-09-01 21:30 (UTC)                                            |
| Agent    | Devin (scheduled daily Playwright run)                            |
| Trigger  | scheduled                                                         |
| Command  | `npx playwright test` (full suite), then targeted `--repeat-each` |
| Base URL | cog-enterprise-qa.beta.devinenterprise.com                        |
| Scope    | all specs (`setup`, `unauthenticated`, `authenticated`, `member`) |

## Results

| Total | Passed | Failed | Flaky | Skipped |
| ----- | ------ | ------ | ----- | ------- |
| 368   | 316    | 48     | 0     | 4       |

Full-run log: `/home/ubuntu/qa-artifacts-2026-09-01/full-run.log`; per-test artifacts (screenshots,
videos, traces, `error-context.md`) under `tests/playwright/test-results/`. Re-run logs:
`retest1.log` (four candidate re-runs), `retest2.log` (`--repeat-each=3` of the six fixes).
Live probes: `probe.log`, `probe2.log`, `mcp-airtable-detail.png`, `mcp-airtable-dialog.png`,
`auto-macro-roast.png`.

Admin storage state was created this run (`npx playwright test specs/auth.setup.ts`, 37 s).
Member auth was skipped — no member credential is configured — so the whole `member` project
failed before executing.

## Failure triage

| Test ID                                                | Error (short)                                                                  | Classification                    | Evidence                                                                                                                       | Action                                                    |
| ------------------------------------------------------ | ------------------------------------------------------------------------------ | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------- |
| AUTH-SAN04, AUTH-REG01, AUTH-REG02, AUTH-REG05         | OTP field never appears / 120 s timeout                                        | infrastructure                    | `test-results/authenticated-auth-*/error-context.md`                                                                           | none — login OTP quota on the shared inbox                |
| MBR-REG01…REG07                                        | `Error reading storage state from .auth/member.json`                           | infrastructure                    | `full-run.log` 42–48                                                                                                           | reported — no member credential configured                |
| MEMB-E2E01                                             | invite mail never arrives (Gmail OTP poll)                                     | infrastructure                    | `full-run.log` 15                                                                                                              | none — shared inbox / quota                               |
| REV-E2E01                                              | `GitLab GET /merge_requests?per_page=1 failed: 401`                            | infrastructure                    | `full-run.log` 21                                                                                                              | reported — `GITLAB_QA_API_TOKEN` rejected (recurring)     |
| SUB-OS-SMK01                                           | console error `Failed to load resource … 429`                                  | infrastructure / flaky            | passes unchanged on re-run (`retest1.log`)                                                                                     | none — server-side rate limit during the cold load        |
| PREF-REG03                                             | `Play sound with notifications` read `false` after flip                        | flaky / state-dependent           | passes unchanged on re-run (`retest1.log`)                                                                                     | none — watch next run                                     |
| SKILL-SMK01/SAN01/SAN02/REG01/REG02/REG03/REG05/REG06  | Skills heading / table / breadcrumb absent; page-level timeouts                | app issue — feature removed       | recurring since 2026-08-31; `/settings/enterprise-skills` 404                                                                  | reported (APP-15) — specs left failing, awaiting decision |
| ENTSET-SAN05, ENTSET-REG01, SUB-OS-SAN01, SUB-OS-REG01 | `Skills & Rules` entry missing from Resources / sub-org settings root          | app issue — same removal          | `test-results/authenticated-ent-*`, `authenticated-s-settings-*`                                                               | reported (APP-15)                                         |
| AUTO-E2E01, SUB-AU-E2E01, SCHED-E2E01                  | automation runs never reach `Succeeded`; no session spawned                    | app issue                         | `full-run.log` 6–8; recurring since 2026-08-24                                                                                 | reported                                                  |
| GUARD-REG04                                            | block banner never renders (warn does)                                         | app issue                         | `full-run.log` 11                                                                                                              | reported (recurring)                                      |
| KNOW-E2E01                                             | Usage tab never lists the triggered session                                    | app issue                         | `full-run.log` 13                                                                                                              | reported (recurring)                                      |
| STOOL-SMK01                                            | Progress tab exposes no `tabpanel` role                                        | app issue                         | `full-run.log` 40; recurring since 2026-08-27 (APP-13)                                                                         | reported                                                  |
| ENV-REG03                                              | `Outposts` heading absent                                                      | app issue                         | `full-run.log` 10; recurring since 2026-08-18                                                                                  | reported                                                  |
| PCON-SMK01, PCON-SAN01, PCON-REG01                     | Slack / GitHub personal-connection rows absent                                 | app/environment (config)          | live probe `probe2.log` — GitLab, self-hosted GitLab, Linear only                                                              | reported — Slack/GitHub not connected at enterprise level |
| REPO-REG01, REPO-REG03, REPO-E2E01                     | permission row missing; drawer timeout; composer repo list empty               | app/environment                   | `full-run.log` 18–20                                                                                                           | reported — QA repo fixture/provider unavailable           |
| PLAY-REG12, PLAY-E2E01                                 | launched session text never contains the playbook marker                       | app issue                         | `full-run.log` 16–17 (ACU limit was `No limit` this run, so not state leakage)                                                 | reported                                                  |
| KNOW-SAN03, MEMB-SMK01, ORGSEL-REG10, SUP-REG01        | docs links resolve to `docs.devinenterprise.com`, specs expect `docs.devin.ai` | script — outdated assumption      | all three doc URLs return HTTP 200; enterprise build ships its own docs site                                                   | fixed (shared `DOCS_ORIGIN` / `DOCS_URL_PATTERN`)         |
| AUTO-REG04                                             | `option` named `/Roast Commits/` not found                                     | script — stale fixture assumption | live probe: the `Roast Commits` playbook no longer exists; option labels are macros (`!roaster_hulk`) — `auto-macro-roast.png` | fixed (page object reads a real playbook)                 |
| ECON-MCP-REG02                                         | switch not found after click; `aria-checked` never `false`                     | script — flow drift               | hiding an MCP now opens `Hide Airtable from the marketplace?` (`mcp-airtable-dialog.png`)                                      | fixed (page object confirms the dialog)                   |

### Verified app behaviour behind the two new script fixes

- Playbook macro mentions **work**: typing `!roast` in the Instructions editor lists
  `!roaster_hulk`, and picking it inserts a chip linking to
  `/settings/playbooks/<id>` labelled `roaster hulk`. Only the hard-coded
  `Roast Commits` fixture is gone.
- MCP marketplace visibility **works**: the switch is `Show in marketplace`; turning it off
  opens a confirmation dialog ("1 organization has an existing installation that will keep
  working…"), Cancel leaves it on, and `Hide from marketplace` persists across a reload.

## Actions taken

- PR opened: `fix/playwright-drift-2026-09-01` — docs origin drift (4 tests), AUTO-REG04
  playbook fixture, ECON-MCP-REG02 confirmation dialog. All six pass `--repeat-each=3`
  (`retest2.log`, 18/18).
- App issues flagged, left failing (no PR, per policy): Skills & Rules removal (12 cases),
  automations never reach `Succeeded`, guardrail block outcome missing, Knowledge Usage never
  lists the session, Progress panel lacks `tabpanel`, Environment has no Outposts tab, playbook
  mode/macro not reflected in launched sessions, personal Slack/GitHub rows absent, repository
  fixture/provider unavailable.
- Quarantine / skip recommendations: none.

## Enterprise sweep (cognition-e2e-platform skills, browser)

Target `https://cog-enterprise-qa.beta.devinenterprise.com` (enterprise `cog-enterprise-qa`,
sub-org `jeet-devin-qa`). Authenticated with the run's admin storage state injected over CDP —
no OTP consumed. Read-only apart from one repository-permission add that was reverted in-case.
Skills read from `/home/ubuntu/repos/cognition-e2e-platform/.agents/skills/`
(`devin-enterprise-web-coordinator`, every `devin-enterprise-web-settings-*`,
`devin-enterprise-web-exploration`, `qa-interaction-matrix`).

Full report: `/home/ubuntu/qa-artifacts-2026-09-01/enterprise-sweep-report.md`
Recording: `/home/ubuntu/screencasts/ent-sweep-2026-09-01/ent-sweep-2026-09-01-edited.mp4`
Screenshots: `/home/ubuntu/qa-artifacts-2026-09-01/shots/`

### Skipped as already covered and green this run

Settings hub, general, connections (integrations list + MCP list/detail/marketplace
visibility), enterprise sessions, Devin, review settings UI, knowledge list/CRUD/search/
unsaved-guard, playbooks CRUD/search/versioning, environment Rollout/Configuration/Blueprint
(ENV-REG10/ENV-REG11 already cover skill TC-ENV-016/017), secrets, security, guardrails,
analytics baseline, infrastructure, support, Devin API baseline, membership groups/roles/
members list, organizations list/search/ACU, preferences, wiki, login/logout, sub-org settings
root.

### Executed in the browser

| Case                                                                             | Verdict                                                                                |
| -------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| A1 Skills & Rules route removal                                                  | FAIL / reclassify — route 404s but the surface was relocated (F5)                      |
| A2 Environment → Outposts absent                                                 | PASS (absent; deep link falls back safely)                                             |
| A3 Personal `/settings/connections` providers                                    | PASS (GitLab, self-hosted GitLab, Linear render; no Slack/GitHub rows is a config gap) |
| A4 Repositories org selector / filters / empty & no-match / add-permission panel | PASS                                                                                   |
| A5 Session Progress `role="tabpanel"`                                            | FAIL — F2                                                                              |
| A6 Membership invite-modal validation                                            | FAIL — F3                                                                              |
| A7 Member / permission-gated views (MBR-\*)                                      | NOT TESTED — no member credential; creating one consumes rate-limited OTP quota        |
| B1 TC-ENV-019 blueprint caret & keyboard nav                                     | PASS                                                                                   |
| B2 TC-CONN-006 auto-copy git-permissions toggle                                  | NOT TESTED — toggle absent from the enterprise Connections surface                     |
| B3 TC-CONN-012 MCP add-server validation                                         | FAIL on malformed URL — F1; long-name layout and cancel-without-create PASS            |
| B4 TC-DAPI-006 Devin API search sanitization                                     | PASS                                                                                   |
| C1 Deep-link / URL abuse                                                         | PASS                                                                                   |
| C2 Playbooks dirty-form navigation                                               | FAIL — F4                                                                              |
| C3 Analytics invalid / future date ranges                                        | PASS                                                                                   |
| C4 Rapid back ×3 / forward ×3 across Membership/Environment/Analytics            | PASS                                                                                   |

### Findings

- **F1 — MCP "Add a custom MCP server" accepts a malformed URL.** Settings → Connections →
  MCP → Add a custom MCP server, URL `not-a-url`. Expected inline validation or a disabled
  **Create server**; actual Create server enabled with no error. `http://localhost:8080` only
  warns ("Loopback URL") and stays creatable. No server was created.
  Evidence: `shots/B3-mcp-not-a-url-CREATE-ENABLED.png`, `shots/B3-mcp-loopback-warning.png`.
- **F2 — Session Progress content is not in a `role="tabpanel"`.** Settings → Sessions → open
  a session → Progress. Expected the active tab content wrapped in `role="tabpanel"`; actual
  content renders with no tabpanel role. Confirms `STOOL-SMK01` as a real accessibility defect.
  Evidence: `shots/A5-progress-tab-no-tabpanel.png`.
- **F3 — Invite modal accepts malformed emails.** Settings → Membership → Members → Invite
  members, type `not-an-email` then `qa-temp-invalid@`. Expected **Add** disabled or an inline
  error; actual Add enabled with no error. Nothing submitted. Reproduces yesterday's finding.
  Evidence: `shots/A6-invite-not-an-email-ADD-ENABLED.png`,
  `shots/A6-invite-trailing-at-ADD-ENABLED.png`.
- **F4 — Playbook draft silently lost on browser Back.** Settings → Playbooks → Create, type
  name `qa-temp-explore-1788305200` plus a body, browser Back, then Forward. Expected an
  unsaved-changes guard or a preserved draft; actual Back leaves with no guard and Forward
  shows an empty pristine form. Nothing was saved.
  Evidence: `shots/C2-playbook-draft-dirty.png`, `shots/C2-back-no-guard-list.png`,
  `shots/C2-forward-draft-LOST.png`.
- **F5 (test-suite issue, not a product bug) — Skills & Rules was relocated, not removed.**
  `/settings/enterprise-skills` 404s and the sidebar entry is gone, but the Skills analytics
  surface (Usage over time, Most invoked skills, Task types, runtime/period filters, skills
  table) now lives at `/org/cog-enterprise-qa/settings/analytics?tab=skills`. The 12 red
  `SKILL-*` / `ENTSET-*` / `SUB-OS-*` cases must be re-pointed at that tab — this run's
  "surface removed" classification above is superseded. Not fixed in this PR: re-pointing
  those specs also needs catalog metadata updates, so it is filed as the top follow-up.
  Evidence: `shots/A1-enterprise-skills-404.png`,
  `shots/A1-skills-analytics-relocated-under-analytics.png`.

### Corrections to earlier assumptions

- `/org/kush-101/...` is **not** a cross-tenant leak: `kush-101` is the QA admin account's own
  home org (sidebar "Back to app" points at it and it appears in its Organizations list). Any
  future IDOR control must use a genuinely foreign org.

## Learnings for next run

- The enterprise build links to `docs.devinenterprise.com`, not `docs.devin.ai`. Assertions now go
  through `DOCS_ORIGIN` / `DOCS_URL_PATTERN` in `support/paths.ts` (overridable via `DOCS_ORIGIN`).
- Playbook mention options are labelled with the **macro** (`!roaster_hulk`), while the inserted
  chip is labelled with the **playbook name**. Never hard-code a playbook that only exists as
  shared QA data — read one from the Playbooks list instead.
- Hiding an MCP from the enterprise marketplace is a two-step confirm; the modal removes the
  switch from the accessibility tree while it is open.
- `jeet-devin-qa` started this run with `No limit` ACU, so none of yesterday's cascade recurred.
- The shared enterprise carries a lot of leftover `qa-temp-*` playbooks/knowledge from earlier
  runs — cleanup in those specs is not reliably completing.
