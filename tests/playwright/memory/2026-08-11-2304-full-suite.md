# Run memory — 2026-08-11 23:04 — full-suite

## Run metadata

| Field    | Value                                                                           |
| -------- | ------------------------------------------------------------------------------- |
| Date     | 2026-08-11 23:04 (UTC)                                                          |
| Agent    | Devin (scheduled daily Playwright run)                                          |
| Trigger  | scheduled                                                                       |
| Command  | `npx playwright test` (then re-run spec-by-spec so each chunk re-authenticates) |
| Base URL | cog-enterprise-qa.beta.devinenterprise.com                                      |
| Scope    | all specs — unauthenticated, authenticated, member                              |

The single full-suite invocation loses its admin session part-way through (the QA session expires
well before the ~3 h suite completes), so the authoritative numbers below come from a spec-by-spec
re-run where the `setup` project re-authenticates before every spec file.

## Results

| Total | Passed | Failed | Flaky | Skipped |
| ----- | ------ | ------ | ----- | ------- |
| 440   | 339    | 61     | 0     | 40      |

After the fixes in this run's PR plus targeted re-runs, 29 of those 61 are green
(27 fixed, 2 confirmed flaky), leaving 32 open.

## Failure triage

### Fixed in this run (script — locator/assumption drift, verified with `--repeat-each=3`)

| Test ID(s)                                                                                     | Error (short)                                        | Classification                 | Evidence                                                                                                                                 |
| ---------------------------------------------------------------------------------------------- | ---------------------------------------------------- | ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| ORG-SAN02, ORG-REG05, ORG-REG07, ORG-REG08, ORG-REG09, ORG-REG12                               | `locator('#displayName')` not found                  | script — locator drift         | Manage dialog inputs now use generated ids + `name="displayName"` / `name="maxAcuLimit"`                                                 |
| ECON-SMK01, ECON-SAN02, ECON-REG02                                                             | expected `MCP servers 101`, got `160`                | script — stale data assumption | MCP catalogue grows over time; badge is now read dynamically and only asserted to be stable                                              |
| KNOW-REG07, KNOW-REG09, KNOW-REG11, KNOW-REG12, KNOW-REG13, KNOW-REG18, KNOW-SAN06, KNOW-REG14 | `getByRole('cell', { name, exact: true })` not found | script — locator drift         | name cell now also holds the row checkbox (`aria-label="Select note <name>"`), so the cell's accessible name is no longer the entry name |
| REV-SAN02                                                                                      | `button[data-index]` not found                       | script — locator drift         | repo rows in "Select repositories" are now `div[role="button"][data-index]`                                                              |
| SUB-AU-REG01                                                                                   | heading `Agent mode` not found                       | script — product change        | `Agent mode` and `Run as` (was `Run as creator`) are labelled selects, not headings                                                      |
| KNOW-SAN01, KNOW-SAN07, KNOW-SAN09, KNOW-REG16, KNOW-E2E01                                     | `Built-in knowledge` row absent                      | script — product change        | product-confirmed removal: System knowledge now expands to `Repo indexes` only, so the read-only System entry under test is a repo index |
| ENV-SMK01, ENV-SAN01, ENV-REG03                                                                | `Outposts` tab absent                                | script — product change        | product-confirmed removal: Environment renders only `Rollout`, `Configuration`, `Blueprint`; ENV-REG03 (outposts-only) is retired        |

### Flaky / infrastructure

| Test ID(s)           | Error (short)                         | Classification                      | Evidence                                                                                                                                      |
| -------------------- | ------------------------------------- | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| API-REG03, API-REG04 | 120 s timeout in service-user cleanup | infrastructure — backend latency    | pass 3/3 on re-run; the QA backend intermittently stalls on service-user create/delete                                                        |
| MBR-REG01…MBR-REG07  | `ENOENT .auth/member.json`            | infrastructure — missing credential | the scheduled `.env` has no `DEVIN_MEMBER_EMAIL`, so `auth.setup` skips member login and the whole `member` project fails instead of skipping |

### Product-surface changes — need a human/product decision before any test change

| Test ID(s)                                             | Error (short)                                              | Evidence                                                                                                                 |
| ------------------------------------------------------ | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| ORGSEL-SAN09, ORGSEL-REG12, ORGSEL-REG13, ORGSEL-REG17 | palette group `Navigation` / `Switch organization…` absent | palette now reads `Actions / Go to… / Change theme… / Toggle sidebar`; row overflow menu no longer has `Manage settings` |
| PLAY-REG07, PLAY-REG12                                 | mode option `Normal` absent                                | playbook Devin-mode select no longer offers `Normal`                                                                     |
| PLAY-REG15                                             | text `Overview` absent on detail                           | playbook detail tab content renamed                                                                                      |
| COMP-REG02                                             | menu item `Codebase files` absent                          | composer "Add context" menu items renamed                                                                                |
| PCON-SMK01                                             | `No MCPs` absent                                           | personal Connections now lists personal MCPs                                                                             |
| SCAN-REG01                                             | link `Create manually` absent                              | Security → Profiles create flow changed                                                                                  |
| ENTSET-REG03                                           | h2 `Enterprise Settings` absent after Back                 | back navigation from the app lands on a different settings surface                                                       |

### QA tenant data drift — data needs restoring (or the case needs a fixture)

| Test ID(s)                            | Error (short)                                         | Evidence                                                                       |
| ------------------------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------ |
| REPO-REG01, REPO-E2E01                | `primary-project` missing from repo list and composer | composer only offers `software-quality-assurance-group/…`                      |
| SESS-REG01, SESS-REG03                | search `kuberns` returns "No sessions found"          | no matching session left in the tenant                                         |
| SKILL-SAN01, SKILL-REG01, SKILL-REG03 | table shows "No skill invocations yet" (last 30 days) | `exploratory-webapp-qa` has no invocation in the window                        |
| PCON-SAN01, PCON-REG01                | GitLab row shows `jeet-qa` / `Unlink user`            | the personal GitLab account is now linked, so the link flow can't be exercised |
| KNOW-REG15                            | zero `View session` buttons                           | the `hi` entry has no usage sessions in the analytics window                   |

### Still to triage

| Test ID(s)             | Error (short)                                         | Note                                                                           |
| ---------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------ |
| SESS-SAN02, SESS-REG02 | Display menu resolves to the inert backdrop element   | menu locator (`getByRole('menu').or('[data-open]')`) matches the overlay first |
| SESS-REG03             | filters from earlier tests persist into the next test | test-isolation gap: enterprise Sessions filters survive across tests           |
| COMP-REG04, REV-E2E01  | cleanup/archive timeouts                              | long E2E paths; not reproduced in isolation yet                                |

## Actions taken

- PR opened: `fix/playwright-locator-drift-2026-08-12` (19 tests fixed, all re-run 3× green).
- App issues flagged: none confirmed. Everything examined is locator/assumption drift, a product
  surface change, QA-tenant data drift, or backend latency. The suspected product defect from
  2026-07-24 (REV-SAN02 "Select repositories" dialog rendering no rows) is resolved in the app —
  the rows render and only the row locator changed.
- Quarantine / skip recommendations: none. The 40 remaining failures stay red until product
  confirms the intended behavior or the QA tenant data is restored.

## Learnings for next run

- Run the suite spec-by-spec (setup re-runs per chunk). A single `npx playwright test` invocation
  outlives the QA session and produces cascading, uninterpretable failures after ~30 minutes.
- Table name cells now carry the row's selection checkbox — never match a row by the cell's
  accessible name; match on the exact cell text instead.
- Dialog inputs no longer have stable ids; use `getByLabel` inside the dialog.
- Do not hard-code tenant counts (MCP catalogue, org totals) — read them and assert stability.
- Add `DEVIN_MEMBER_EMAIL` to the scheduled `.env` or the `member` project will fail rather than
  skip.

## Enterprise sweep (cognition-e2e-platform)

Source: `ent-qa/cognition-e2e-platform` `.agents/skills/devin-enterprise-web-settings-*` — 241 `TC-*`
cases across 22 skills, diffed against this repo's 308 catalog cases / 440 Playwright tests.
Full report: `/home/ubuntu/enterprise-sweep-results.md`; recordings under
`/home/ubuntu/screencasts/enterprise-sweep*`.

### Skipped as already covered

226 of the 241 enterprise cases map 1:1 onto catalog cases the Playwright suite ran this session
(analytics, connections, devin-api, devin, general, guardrails 1-5/7-9, hub, infrastructure,
knowledge, groups, members, roles, organizations, playbooks, repositories 1-8/10-12, review,
secrets 1-7/9-12, sessions, skills-rules, support 1/2/4/5). The whole `security` skill
(TC-SECU-001…009) is moot: the route does not exist on this build (ESECU-REG01).

### Executed — no Playwright equivalent, or the equivalent is manual/not-automatable

| Case                                        | Verdict                   | Note                                                                                                   |
| ------------------------------------------- | ------------------------- | ------------------------------------------------------------------------------------------------------ |
| TC-ENV-016 blueprint unsaved-changes guard  | **fail** (high)           | silent data loss on navigate away                                                                      |
| TC-ENV-017 blueprint YAML validation        | **fail** (high)           | Save stays enabled on invalid YAML, no inline error                                                    |
| TC-ENV-018 dirty state / Discard round-trip | partial pass (low)        | byte-identical revert, but no confirmation                                                             |
| TC-ENV-019 caret & keyboard navigation      | pass                      |                                                                                                        |
| TC-ENV-012 History tab diff                 | pass                      |                                                                                                        |
| TC-ENV-013 Files tab upload/delete          | pass                      |                                                                                                        |
| TC-ENV-014 Secrets tab masking              | pass                      |                                                                                                        |
| TC-ANLY-009 CSV formula injection           | pass with caveat          | 3 CSVs byte-scanned, 0 offending cells; escaping path unproven — no hostile value exists in the tenant |
| TC-CONN-012 MCP add-custom validation       | **fail** (medium)         | invalid URL/host/name inputs not rejected as expected                                                  |
| TC-ROLE-013 permission matrix a11y          | pass                      |                                                                                                        |
| TC-MEM-007 self-demotion guard              | pass                      |                                                                                                        |
| TC-SEC-008 secret import race idempotency   | pass                      | 3 rapid Store clicks → exactly 1 secret; cleaned up                                                    |
| TC-GRD-006 violation detail data leak       | not_tested                | no detail view exists on this build                                                                    |
| TC-SUP-003 support chat widget              | **fail/blocked** (medium) | `POST /api/support/decagon-auth` → 404                                                                 |
| TC-REPO-009 Manual Config (Advanced) IDOR   | pass                      | foreign `?org=` ignored; foreign org path → clean 404                                                  |

### Exploratory (in neither catalog)

| Charter                                                                                              | Verdict                                                            |
| ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| C1 deep-link abuse (`?tab=GROUPS`, `?tab=%00`, unknown route)                                        | pass — clean fallback / in-app 404                                 |
| C2 cross-tab isolation of unsaved blueprint edits                                                    | pass                                                               |
| C3 hostile search input, keyboard                                                                    | pass (ASCII payload only)                                          |
| C3b hostile search input via `page.fill` — 10k chars, RTL, emoji/ZWJ, zero-width, mixed HTML+formula | pass — literal render, no dialog, no overflow, correct empty state |
| C4 modal focus trap + Escape does not commit                                                         | pass                                                               |

All mutations used `qa_temp_<ts>` naming and were deleted and verified gone; no pre-existing record
was modified.

### Coverage this sweep suggests adding to the catalog

1. Blueprint editor dirty-state lifecycle (navigate-away guard, YAML validation gating Save, Discard round-trip).
2. Secret import idempotency under rapid repeat submit, with delete-and-reload verification.
3. Cross-tab isolation of unsaved editor state.
4. Deep-link robustness matrix (bad enum tab value, `%00`, unknown route, foreign org slug).
5. Modal focus-trap + Escape-does-not-commit for Invite members and Create role.
6. Analytics CSV export: download-and-parse (no cell starts with `=`/`+`/`-`/`@`/TAB) plus KPI-vs-CSV reconciliation.
7. Search hostile-input matrix driven programmatically rather than by keyboard — xdotool silently drops
   RTL, emoji, zero-width and very long strings, so the keyboard path under-tests these fields.

### Open questions for the product/QA lead

- Support chat 404: not configured for this tenant, or a regression?
- Guardrails violation detail view is absent on this build — retire TC-GRD-006 or update the skill?
- TC-ANLY-009 escaping proof needs a record named `=1+1_qa-temp-<ts>` in a disposable tenant; not run here
  because it breaches the non-destructive rule in `cog-enterprise-qa`.
