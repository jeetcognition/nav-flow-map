# Run memory

One committed markdown file per agent-driven test run, named `YYYY-MM-DD-HHmm-<scope>.md`.
The required template and triage rules live in [`../PLAYWRIGHT-AGENT.md`](../PLAYWRIGHT-AGENT.md)
(“Per-run memory”). Skim the newest files here before a run to pick up known issues and open PRs.
