# Run memory — 2026-09-07 21:30 — full-suite

## Run metadata

| Field    | Value                                                                              |
| -------- | ---------------------------------------------------------------------------------- |
| Date     | 2026-09-07 21:30 (UTC)                                                             |
| Agent    | Devin (scheduled daily Playwright run)                                             |
| Trigger  | scheduled                                                                          |
| Command  | `npx playwright test specs/auth.setup.ts`, then `npx playwright test` (full suite) |
| Base URL | cog-enterprise-qa.beta.devinenterprise.com                                         |
| Scope    | all specs (`setup`, `unauthenticated`, `authenticated`, `member`)                  |

## Results

| Total | Passed | Failed | Flaky | Skipped |
| ----- | ------ | ------ | ----- | ------- |
| 368   | 289    | 75     | 0     | 4       |

Gmail IMAP worked again (the 2026-09-06 credential failure is resolved) and `.auth/admin.json`
was regenerated. Member setup skipped (`DEVIN_MEMBER_EMAIL` unset). Full run took 1.2 h.
Artifacts: `/home/ubuntu/qa-artifacts-2026-09-07/` (`full-run.log`, `results.json`,
`failures.tsv`, `reruns/test-results-rerun{1..4}`, `enterprise/`); repo `test-results/`,
`playwright-report/`.

Mid-run root cause: `ORG-REG07` failed on an ACU-cell assertion (see drift below) and left
`jeet-devin-qa` at **Monthly cloud ACU limit = 0**. Every later session-spawning spec then
failed ("34 of your sessions were stopped by a usage limit" banner), and every later
`e-orgs` assertion on `No limit` failed. The limit was restored to No limit manually via the
Manage dialog (PATCH 200, `probe/restore-acu.mjs`) before re-runs.

## Failure triage

Re-run 1 (after ACU restore, 25 tests): 17 passed / 8 failed → those 17 are classified as
state-cascade. Re-run 4 (`--repeat-each=3` on the fixed specs) is in "Actions taken".

| Test ID(s)                                                                                                                                                                       | Error (short)                                                                                 | Classification                        | Evidence                                                    | Action                             |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------- | ----------------------------------------------------------- | ---------------------------------- |
| AUTH-REG01, AUTH-REG02, AUTH-REG05                                                                                                                                               | OTP field never shown; page says "Rate limit exceeded"                                        | infrastructure (OTP quota)            | `test-results/authenticated-auth-*`                         | reported                           |
| MBR-REG01…REG07                                                                                                                                                                  | `.auth/member.json` missing                                                                   | infrastructure (no member credential) | run log                                                     | reported (recurring)               |
| ORG-SMK01                                                                                                                                                                        | header expects "Monthly ACU limit", UI says "Monthly cloud ACU limit"                         | script — confirmed UI copy change     | `probe/cell-dom.mjs` output, rerun logs                     | fixed                              |
| ORG-REG05, ORG-REG07                                                                                                                                                             | cell `toHaveText("7")` got `7Cloud only` (number + "Cloud only" badge)                        | script — confirmed UI drift           | `failures.tsv`, `probe/cell-dom.mjs` innerHTML              | fixed (page-object helper)         |
| ORG-REG08, ORG-REG11                                                                                                                                                             | expected `No limit`, got `0Cloud only`                                                        | state cascade from ORG-REG07          | rerun 4 passes once REG07 cleans up                         | fixed via REG07 cleanup            |
| ORG-SAN02, ORG-SAN03, ORG-REG01, ORG-REG02, ORG-REG03, ROLE-REG08, ROLE-REG11, ORGCRT-REG07/08/10                                                                                | console `429` resource errors                                                                 | infrastructure (rate limiting)        | rerun 1 all pass                                            | reported                           |
| COMP-SMK02, COMP-REG04, PLAY-REG12, GEN-REG04, KNOW-REG08                                                                                                                        | session URL never reached / unstable toggle / Next enabled                                    | state cascade (ACU 0) / flaky         | rerun 1 all pass                                            | none                               |
| KNOW-REG17                                                                                                                                                                       | `getByText("Not Found")` — app now renders `404` + "This page could not be found"             | script — 404 page copy change         | screenshot in `test-results/*KNOW-REG17*`                   | fixed                              |
| SCHED-REG01, AUTO-E2E01, SUB-AU-E2E01, SCHED-E2E01, KNOW-E2E01, PLAY-E2E01, MEMB-E2E01, REV-E2E01, REPO-E2E01                                                                    | session/automation never completes; editor 403 `PUT /api/vscode-server/*`; usage-limit banner | app/environment (sub-org quota)       | rerun 1 screenshots                                         | reported (APP-16)                  |
| SCAN-SMK01                                                                                                                                                                       | `/security` no longer shows "Access denied"; redirects to org selector / sub-org Security     | app behavior change (unconfirmed)     | `reruns/test-results-rerun1/*SCAN-SMK01*`                   | reported, not patched              |
| SCAN-REG01                                                                                                                                                                       | after `page.reload()` on `/org/jeet-devin-qa/security` the composer rendered, not Security    | inconclusive (see APP-17)             | `reruns/test-results-rerun1/*SCAN-REG01*/test-failed-1.png` | reported                           |
| STOOL-SMK01                                                                                                                                                                      | no `role=tabpanel` for Progress                                                               | app (a11y, recurring APP-13)          | rerun 1                                                     | reported                           |
| SUB-WK-REG01, DWIKI-SMK01, DWIKI-REG01                                                                                                                                           | repo `jeetcognition/empty` not in Wiki list                                                   | environment data (repo missing)       | rerun 1 screenshot                                          | reported                           |
| ANAL-REG07, API-REG06, ECON-REG03, DEVIN-REG07, ENV-REG08, GEN-REG06, GUARD-REG06, INFRA-REG04, ORG-REG13, PLAY-REG13, REV-REG06, ESEC-REG01, SESS-REG06, SKILL-REG05, SUP-REG02 | breadcrumb no longer contains "Enterprise"; some pages show only "Settings"                   | app/UI drift (recurring, APP-14)      | screenshots; enterprise sweep A6                            | reported — awaiting product intent |
| ENV-REG03                                                                                                                                                                        | no Outposts tab (Rollout/Configuration/Blueprint)                                             | app (recurring)                       | sweep A2                                                    | reported                           |
| SKILL-SMK01, SKILL-REG03, SKILL-REG06, ENTSET-SAN05                                                                                                                              | Skills & Rules removed; route redirects to `analytics?tab=skills`; no hub card                | app (recurring APP-15)                | sweep A3                                                    | reported                           |
| SUB-OS-SAN01, SUB-OS-REG01, ENTSET-REG01                                                                                                                                         | sub-org settings inventory / card list drift                                                  | app/UI drift                          | `test-results/*SUB-OS*`                                     | reported                           |
| PCON-SMK01, PCON-SAN01, PCON-REG01                                                                                                                                               | `#personal-slack-link` absent; rows are GitHub, GitLab, Self-hosted GitLab, Linear            | app/UI drift (recurring)              | sweep A4                                                    | reported                           |
| REPO-REG01, REPO-REG03, REV-E2E01                                                                                                                                                | GitLab API 401 for `GITLAB_QA_API_TOKEN`; repo permission flow                                | infrastructure (token)                | error-context                                               | reported                           |
| KNOW-REG19                                                                                                                                                                       | console error during usage tab                                                                | inconclusive (not re-run)             | `test-results/*KNOW-REG19*`                                 | watch                              |

### App issues (bug reports)

**APP-16 — Session-spawning flows blocked / editor 403 (SCHED-REG01, AUTO-E2E01, SUB-AU-E2E01,
SCHED-E2E01, KNOW-E2E01, PLAY-E2E01, MEMB-E2E01, REV-E2E01, REPO-E2E01)**

1. Failing assertion: `waitForResponseEnding(marker)` / `waitForURL(/sessions/)` time out.
2. Repro: as admin in `jeet-devin-qa`, start any session from the composer or a playbook macro.
3. Expected: session reaches "In progress" and Devin responds.
4. Actual: banner "34 of your sessions were stopped by a usage limit that has since been
   raised"; session pane shows "The editor failed to start — HTTPError 403: PUT
   /api/vscode-server/devin-…". Partly caused by ORG-REG07 leaving ACU=0 (now guarded), but
   the 403 and stuck sessions persisted after the limit was restored.

**APP-17 — Sub-org Security/Automations route rendered the New-session composer after reload
(SCAN-REG01, SCHED-REG01)** — inconclusive. Playwright screenshots show URL
`/org/jeet-devin-qa/security` with the composer, but the manual sweep (native F5, same
account, later in the day) rendered the correct pages 4/4 times. Likely tied to the
usage-limit state above. Keep watching.

**APP-18 — `/security` (no org prefix) no longer renders "Access denied" (SCAN-SMK01)**:
redirects to the org selector (Playwright) / to `/org/jeet-devin-qa/security` (sweep).
Probably an intentional redirect; test assertion not changed pending product confirmation.

**APP-20 — Repositories org selection lost on return** (sweep B5). Repro: Repositories →
select `jeet-devin-qa` (`?org=` set) → sidebar Knowledge → sidebar Repositories. Expected:
selection retained. Actual: "Select an organization". Evidence `enterprise/B5-*.png`.

Not bugs (reviewed by Jeet): playbook-editor browser Back discarding unsaved edits (sweep B2)
is accepted behavior; `enterprise-connections?tab=garbage` 404 (sweep B3) is expected because
`/org/<enterprise>/settings/connections` is now the canonical endpoint; the new-secret value
field (TC-ENV-014) is intentionally plaintext while typing and is hidden once the secret is saved.

Recurring, still open: APP-13 (Progress tabpanel), APP-14 (breadcrumb "Enterprise" crumb
removed; Analytics/Skills show only "Settings"), APP-15 (Skills & Rules removed), Outposts
tab removed, Personal Connections inventory drift, Wiki repo `jeetcognition/empty` missing,
Support chat unavailable (`POST /api/support/decagon-auth` 404), Wiki API
`get_full_multi_language_wiki` 404 while page still renders.

## Actions taken

- Script fixes (branch `fix/playwright-org-acu-cleanup-2026-09-07`):
  - `pages/organizations.page.ts`: `acuLimitCell(row)` + `acuLimitText(value)` — the ACU
    cell now renders `<n>` followed by a "Cloud only" badge; `No limit` unchanged.
  - `e-orgs.spec.ts`: header text → "Monthly cloud ACU limit"; all ACU-cell assertions use
    the helper; `ORG-REG07` wraps its mutations in `try/finally` that reloads and restores
    No limit so a failure can no longer leave the sub-org at ACU 0.
  - `e-knowledge.spec.ts` (`KNOW-REG17`): body-level `NOT_FOUND_TEXT =
/Not Found|This page could not be found/` instead of exact `getByText("Not Found")`.
- Re-runs: `--repeat-each=3` on ORG-SMK01/REG05/REG07/REG08/REG11 + KNOW-REG17 → 15/18
  pass; the 3 failures are all 429 rate-limit side effects (list refetch after PATCH not
  refreshed; console 429s) and no longer cascade (REG08/REG11 pass right after a REG07
  failure). Root `npx prettier --check .` clean for changed files (2 pre-existing warnings
  on `main`: `app/src/types.ts`, `memory/2026-09-02-…md`). No catalog change.
- Not patched (app/product): breadcrumbs, Outposts, Skills, PCON, SCAN-SMK01, wiki, e2e.

## Enterprise sweep (cognition-e2e-platform skills, browser)

Repo fetched via GitHub API tarball (git proxy 403). Coverage diff: 242 enterprise TCs across
22 settings skills vs 359 Playwright IDs (`/home/ubuntu/qa-artifacts-2026-09-07/enterprise-tcs.txt`,
`catalog-coverage.json`). Per-area mapping: every settings skill has a Playwright spec
(`e-*.spec.ts`, `ent.spec.ts`, `roles/groups/e-membership`), so all load/breadcrumb/search/
inventory/IDOR/no-error TCs (≈210) were **skipped as already covered and passed** this run.
TC-SECU-\* (enterprise Security page) is N/A: route is an in-app 404 (`ESECU-REG01` passed).

Executed in the browser (admin storage state, target enterprise only; full details and
screenshots in `/home/ubuntu/qa-artifacts-2026-09-07/enterprise/qa-results.md`, recording
`/home/ubuntu/screencasts/enterprise-verified-sweep/enterprise-verified-sweep-edited.mp4`):

| Case                       | Why executed                   | Verdict                                                               |
| -------------------------- | ------------------------------ | --------------------------------------------------------------------- |
| TC-SUP-003 chat widget     | no Playwright equivalent       | BLOCKED — "Support chat is unavailable", auth endpoint 404            |
| TC-ENV-007 tabs            | ENV-REG03 failed               | Rollout/Configuration/Blueprint; no Outposts (confirms app change)    |
| TC-ENV-011 sidebar         | no equivalent                  | PASS                                                                  |
| TC-ENV-012 History diff    | no equivalent                  | PASS                                                                  |
| TC-ENV-013 Files           | no equivalent (inventory only) | PASS                                                                  |
| TC-ENV-014 Secrets masking | no equivalent                  | PASS (by design) — value visible while typing, hidden after save      |
| TC-SKR-001/002             | SKILL-\* failed                | route redirects to `analytics?tab=skills`; breadcrumb only "Settings" |
| TC-CONN-003 (personal)     | PCON-\* failed                 | rows: GitHub, GitLab, Self-hosted GitLab, Linear; no Slack            |
| A5 sub-org reload matrix   | SCAN-REG01/SCHED-REG01 failed  | PASS 4/4 (does not reproduce → APP-17 inconclusive)                   |
| A6 breadcrumbs             | 15 breadcrumb specs failed     | confirmed drift (APP-14)                                              |
| A7 wiki                    | wiki specs failed              | `jeetcognition/empty` absent; other repo wiki renders; API 404        |
| TC-HUB-008, all non-admin  | member cred missing            | BLOCKED (not executed)                                                |

### Exploratory pass (`devin-enterprise-web-exploration` + `qa-interaction-matrix`)

| Cell                                         | Verdict                                                        |
| -------------------------------------------- | -------------------------------------------------------------- |
| Devin settings: pristine / hostile email     | PASS (auto-save surface, no Save/Discard; typeMismatch blocks) |
| Devin settings: dirty × navigate/back/reload | BLOCKED (auto-saves on blur; not mutated)                      |
| Playbook editor: dirty × browser Back        | Back discards unsaved edits — accepted behavior, not a bug     |
| Playbook editor: undo / dirty × Cancel       | PASS (Cancel shows Discard dialog)                             |
| Deep link `?tab=garbage` connections         | 404 on legacy `enterprise-connections` route — expected        |
| Deep link `?tab=<script>` membership         | PASS (falls back to Members)                                   |
| Back/Forward across 3 settings pages         | PASS                                                           |
| Org search 300 chars / emoji; Sessions empty | PASS (explicit empty states, no console errors)                |
| Repositories org selection × cross-page nav  | **FAIL — selection lost (APP-20)**                             |

## Learnings for next run

- A failed `e-orgs` mutation can poison the whole run (ACU 0 ⇒ no sessions). Now guarded in
  ORG-REG07; consider the same reload-then-restore pattern for ORG-REG05.
- The enterprise QA tenant rate-limits (`429`) under repeat load; run org specs serially and
  don't `--repeat-each` them against the live tenant more than needed.
- `--no-deps` when re-running `authenticated` specs avoids burning Gmail OTPs.
- Browser sweep login: restore both cookies **and** localStorage from `.auth/admin.json`.
- `ent-qa/cognition-e2e-platform` clone still 403s through the proxy; the API tarball with
  `GITHUB_NAVFLOW_PAT` works.
