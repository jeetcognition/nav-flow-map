# Run memory — 2026-08-29 21:30 — full-suite

## Run metadata

| Field    | Value                                                                                |
| -------- | ------------------------------------------------------------------------------------ |
| Date     | 2026-08-29 21:35 → 23:05 (UTC)                                                       |
| Agent    | Devin (automation session, scheduled daily run)                                      |
| Trigger  | scheduled                                                                            |
| Command  | `npx playwright test` (executed as 6 chunks, each preceded by `specs/auth.setup.ts`) |
| Base URL | cog-enterprise-qa.beta.devinenterprise.com                                           |
| Scope    | all specs (`setup`, `unauthenticated`, `authenticated`, `member`)                    |

As on 2026-08-24/26/27, a single process cannot finish the suite: the shared admin storage state
expires mid-run and later specs redirect to `/auth/login`. The run was executed as 6 sequential
`--no-deps` chunks (fewer than the 10 used on 2026-08-27, to reduce OTP re-authentications),
re-authenticating before each chunk.

## Results

| Total | Passed | Failed | Flaky | Skipped |
| ----- | ------ | ------ | ----- | ------- |
| 366   | 325    | 38     | 0     | 3       |

Skipped: `ENV-REG04`, `ENV-REG05` (unmet preconditions) plus one unauthenticated precondition
case, and the member auth setup step (no member credential — INFRA-1).

Artifacts (local, not committed): `/home/ubuntu/qa-artifacts-2026-08-29/` — `json/c1..c6.json`,
`blob/c1..c6.zip`, `logs/`, plus per-failure `tests/playwright/test-results-c1..c6/`
(screenshots + videos + `error-context.md` a11y snapshots), fix verification under
`test-results-fix1..fix4/` and classification probes under `test-results-probe1/`.

One more failure than 2026-08-27 (37 → 38): the two ENTSET locator fixes from #178 held, while
`AUTH-SAN04` regressed back to red (OTP step unreachable — the shared inbox was rate-limited) and
`MEMB-REG05` / `PREF-REG04` went red on stale live-data assumptions (fixed in this PR).

## Failure triage

Classification key: **script** = locator drift / stale assumption (fixed in this PR),
**app** = product behavior defect (reported only, test left failing), **infra** = environment,
credential, or fixture gap, **inconc** = inconclusive.

| Test ID                      | Error (short)                                                | Classification | Evidence                                                                                        | Action                     |
| ---------------------------- | ------------------------------------------------------------ | -------------- | ----------------------------------------------------------------------------------------------- | -------------------------- |
| MEMB-REG05                   | org cell `0 organizations`, expected `1 organization`        | script         | `test-results-c3/…organizations…/error-context.md`: Albert Han now has 0 orgs; Chris Tian has 1 | discovery by count (fixed) |
| PREF-REG04                   | `expect(current).not.toBe("In-session")`                     | script         | `test-results-c1/…prefs…/error-context.md`: account already held the hard-coded target          | target read from options   |
| SUB-OS-SMK01/SAN01/REG01..03 | 4 console `401`s on cold load                                | app            | APP-1 (repeat of 2026-08-24/26/27)                                                              | reported                   |
| SECRET-SMK01                 | console `401` on sub-org Secrets cold load                   | app            | APP-1                                                                                           | reported                   |
| SCAN-SMK01                   | console `401` on Security cold load                          | app            | APP-1                                                                                           | reported                   |
| ENV-REG03                    | `heading "Outposts"` absent                                  | app            | APP-7 (repeat) — probe 3/3 deterministic (`test-results-probe1/`)                               | reported                   |
| ENTSET-REG03                 | Enterprise Settings heading absent after browser Back        | app            | APP-6 (repeat, intermittent) — probe 1/3 red, 2/3 green                                         | reported                   |
| STOOL-SMK01                  | `getByRole("tabpanel", { name: /Progress/ })` not found      | app            | APP-13 (repeat of 2026-08-27)                                                                   | reported                   |
| AUTO-E2E01                   | webhook run: no `Succeeded` event                            | app/inconc     | APP-5 (repeat; sub-org still reports usage-limit-stopped sessions)                              | reported                   |
| SUB-AU-E2E01                 | schedule run: no `Succeeded` event                           | app/inconc     | APP-5                                                                                           | reported                   |
| SCHED-E2E01                  | run-once schedule: no `Succeeded` event                      | app/inconc     | APP-5                                                                                           | reported                   |
| GUARD-REG04                  | flagged-message banner never rendered (60 s)                 | inconc         | needs a live agent response; sessions stay in setup on this tenant                              | assertion left intact      |
| ECON-E2E01                   | session never printed `MCP-CONTEXT7-AVAILABLE` (300 s)       | inconc         | agent-session dependent (repeat)                                                                | assertion left intact      |
| KNOW-E2E01                   | session never printed `KNOWLEDGE-TRIGGER-CONFIRMED` (180 s)  | inconc         | as above                                                                                        | as above                   |
| PLAY-E2E01                   | session never printed `PLAYBOOK-TRIGGER-CONFIRMED-…` (240 s) | inconc         | as above                                                                                        | as above                   |
| AUTH-SAN04                   | `heading /verify your identity/` absent                      | infra          | INFRA-2 — OTP step unreachable, shared inbox rate-limited by re-authentication                  | reported                   |
| AUTH-REG01/REG02/REG05       | OTP code field never rendered                                | infra          | INFRA-2                                                                                         | reported                   |
| MEMB-E2E01                   | Gmail OTP timed out (invite acceptance, 120 s)               | infra          | INFRA-2 — reproduced again in the fix re-run (`test-results-fix4/`)                             | reported                   |
| REV-E2E01                    | GitLab `GET /merge_requests` → 401                           | infra          | INFRA-3 (`GITLAB_QA_API_TOKEN` still rejected)                                                  | reported                   |
| REPO-REG01/REG03/E2E01       | QA repo absent from the permissions table / timeout          | infra          | INFRA-4 (repo fixture not granted to this enterprise)                                           | reported                   |
| PCON-SMK01/SAN01/REG01       | `#personal-slack-link` absent                                | infra          | INFRA-5 (Slack/GitHub not connected at enterprise level)                                        | assertions left intact     |
| MBR-REG01..07                | `ENOENT .auth/member.json`                                   | infra          | INFRA-1 (no member credential configured)                                                       | reported (7 tests)         |

### Script fixes verified

- `MEMB-REG05` — `--repeat-each=3`: 3/3 green (`test-results-fix3/`).
- `PREF-REG04` — `--repeat-each=3`: 3/3 green (`test-results-fix1/`).
- Whole-file re-run of `prefs.spec.ts` + `e-membership.spec.ts` (`test-results-fix4/`): 18 passed,
  1 failed — `MEMB-E2E01` only (INFRA-2, Gmail OTP). `PREF-REG03` also passed here, so yesterday's
  APP-4 multi-toggle persistence defect did not reproduce this run.

Old → new locators / assumptions:

| Element                           | Old                                                         | New                                                        |
| --------------------------------- | ----------------------------------------------------------- | ---------------------------------------------------------- |
| Member row with 0 / 1 / many orgs | `memberRow("Aayush Prabhu" / "Albert Han" / "Armaan Dodd")` | `findRowByOrgCount(count => …)` over the live org column   |
| Org-count popover                 | `[role="dialog"]` filtered by `/\d+\s*members/`             | `/\d+\s*members?/` (a 1-org member shows "1 member")       |
| Preferences dropdown target value | hard-coded `"You only"` / `"In-session"` / `"When the PR…"` | `PrefsPage.otherOption(row)` — first live option ≠ current |

## App issues (reported, not patched)

**APP-1 — 401s on cold load of sub-org Settings / Secrets / Security** (repeat)
Tests: `SUB-OS-SMK01/SAN01/REG01/REG02/REG03`, `SECRET-SMK01`, `SCAN-SMK01`; assertion: the
console-errors array is empty.
Repro: log in as enterprise admin → open `/org/jeet-devin-qa/settings` (or `…/settings/secrets`,
`…/settings/security`) with a clean cache while watching the console.
Expected: no failed requests. Actual: 4× `Failed to load resource: … status of 401` per load
(2 on 2026-08-27, so the noise grew).

**APP-5 — Automation runs never reach "Succeeded"** (repeat, root cause still open)
Tests: `AUTO-E2E01` (webhook), `SUB-AU-E2E01` (manual schedule run), `SCHED-E2E01` (run-once).
Repro: create an automation in `jeet-devin-qa`, trigger it (webhook POST with the correct secret
returns `200 {"status":"accepted"}`), poll the automation's events list for up to 7 minutes.
Expected: an event linking to `/sessions/<id>` with status Succeeded. Actual: no Succeeded event;
the spawned session stays in setup. The sub-org still reports sessions stopped by a usage limit, so
this may be quota rather than a scheduler defect — a run with quota headroom is still needed.

**APP-6 — Browser Back after "Back to app" lands on the org selector** (repeat, intermittent)
Test: `ENTSET-REG03` — failed in the main run, reproduced 1/3 in the probe re-run.
Repro: `/org/cog-enterprise-qa/settings` → "Back to app" → browser Back.
Expected: return to Enterprise Settings. Actual: `/org/cog-enterprise-qa/org-selector`.
Evidence: `test-results-probe1/…and-return-via-browser-Back…-repeat2/`.

**APP-7 — Environment page renders no Outposts section** (repeat, deterministic)
Test: `ENV-REG03` — `expect(heading "Outposts").toBeVisible()`; 3/3 in the probe re-run.
Repro: `/org/cog-enterprise-qa/settings/environment`.
Expected: an Outposts section (configuration/list/empty state). Actual: heading absent.

**APP-13 — Session tools panel exposes no `tab`/`tabpanel` roles** (repeat of 2026-08-27)
Test: `STOOL-SMK01` — `expect(session.toolPanel("Progress")).toBeVisible()`.
Repro: open `/org/jeet-devin-qa`, open a session, click the `Progress` launcher.
Expected: the opened tool renders a `role="tabpanel"` linked to its tab.
Actual: the tab strip is a `role="tablist"` of plain buttons and the panel content carries no
`tabpanel` role. Evidence: `test-results-c6/…STOOL-SMK01…/error-context.md`.

## Infrastructure gaps

- **INFRA-1** — no member credential ⇒ `.auth/member.json` missing, 7 `MBR-*` tests cannot run.
- **INFRA-2** — Gmail OTP quota/inbox contention: `AUTH-SAN04`, `AUTH-REG01/REG02/REG05`,
  `MEMB-E2E01` (6 re-authentications per suite run still exhaust it).
- **INFRA-3** — `GITLAB_QA_API_TOKEN` rejected with 401 ⇒ `REV-E2E01`.
- **INFRA-4** — the QA repository fixture is not granted to this enterprise ⇒ `REPO-REG01`,
  `REPO-REG03`, `REPO-E2E01`.
- **INFRA-5** — Slack/GitHub are not connected at the enterprise level, so the personal
  Connections page renders neither row ⇒ `PCON-SMK01/SAN01/REG01`.

## Actions taken

- PR opened from `fix/playwright-membership-prefs-2026-08-29` (`MEMB-REG05`, `PREF-REG04`).
- App issues flagged: APP-1, APP-5, APP-6, APP-7, APP-13 (all repeats; no new app defect from the
  Playwright suite this run).
- Quarantine / skip recommendations: none — no assertion was weakened or skipped.

## Learnings for next run

- Never name enterprise members or fixed org counts: the `cog-enterprise-qa` membership table
  drifts between runs (Albert Han lost his organization since 2026-08-27). Discover the row shape.
- The org-count popover uses singular "1 member" for single-member orgs — plural-only regexes fail.
- Preferences dropdown targets must be derived from the live option list; a previous run's value
  (or an admin change) can already equal a hard-coded target and kill the pre-condition assertion.
- 6 chunks (down from 10) did **not** save the OTP-dependent login tests; the shared inbox is the
  binding constraint, not the chunk count.
- The session-spawning E2E cases (`ECON-E2E01`, `KNOW-E2E01`, `PLAY-E2E01`, `GUARD-REG04`, APP-5
  trio) have now been red for four consecutive runs; they cannot discriminate app defects until the
  tenant can actually run agent sessions.

# Enterprise sweep (cognition-e2e-platform skills)

Target: `https://cog-enterprise-qa.beta.devinenterprise.com` (slug `cog-enterprise-qa`, sub-org
`jeet-devin-qa`). The admin storage state from the Playwright run was injected into Chrome over CDP
(`localhost:29229`) so no email OTP was consumed; no navigation to `app.devin.ai` /
`app.beta.devin.ai` / `staging.itsdev.in`. Read this run: `devin-enterprise-web-coordinator`, all 22
`devin-enterprise-web-settings-*` skills, `devin-enterprise-web-exploration`,
`qa-interaction-matrix`. Strictly non-destructive: nothing was saved, created, deleted, invited or
toggled; forms and dialogs were abandoned (Escape / navigate away). Evidence root:
`/home/ubuntu/qa-artifacts-2026-08-29/ent-sweep/` (+ `PLAN.md`, `network.log`); screen recording:
`/home/ubuntu/screencasts/ent-sweep-2026-08-29/ent-sweep-2026-08-29-edited.mp4`.

## Coverage diff (skills → nav-flow-map catalog/specs)

Skipped as already covered **and green this run**: hub, general, sessions, devin, security,
guardrails (non-session cases), skills-rules, infrastructure, support, membership-groups,
membership-roles, organizations, knowledge, playbooks, analytics, secrets, devin-api — the
corresponding `ENTSET-*`, `EGEN-*`, `ESESS-*`, `EDEV-*`, `ESECU-*`, `EGUARD-*`, `ESKILL-*`,
`EINFRA-*`, `ESUP-*`, `GRP-*`, `ROLE-*`, `ORG-*`, `EKNOW-*`, `EPLAY-*`, `EANLY-*`, `ESEC-*` and
`EAPI-*` specs passed.

Executed this sweep — only areas whose Playwright equivalent failed or has no equivalent:

| Area (skill / exploration)                                      | Why executed                                            |
| --------------------------------------------------------------- | ------------------------------------------------------- |
| environment (C1)                                                | `ENV-REG03` failed (APP-7)                              |
| hub navigation history (C2)                                     | `ENTSET-REG03` failed (APP-6)                           |
| sub-org settings/secrets/security cold load (C3)                | `SUB-OS-*`, `SECRET-SMK01`, `SCAN-SMK01` failed (APP-1) |
| session tools a11y (C4)                                         | `STOOL-SMK01` failed (APP-13)                           |
| repositories (C5)                                               | `REPO-REG01/REG03/E2E01` failed (INFRA-4)               |
| connections, personal + enterprise (C6)                         | `PCON-SMK01/SAN01/REG01` failed (INFRA-5)               |
| `/settings/usage` route (C7)                                    | ENT-22 re-check, no Playwright equivalent               |
| blueprint editor keyboard/Discard (C8)                          | no Playwright equivalent (interaction matrix)           |
| invite-dialog hostile input (C9)                                | no Playwright equivalent for validation of bad e-mails  |
| sub-org quota banner (C10)                                      | root-cause probe for APP-5 / session-spawning E2Es      |
| empty & boundary states (C11)                                   | no Playwright equivalent (exploration)                  |
| uncatalogued pages: Usage Policies, Learning, Devin Coach (C12) | zero coverage in either catalog                         |
| network/console inventory (C13)                                 | quantifies APP-1 and finds new 4xx                      |

## Executed cases and verdicts

| Case                                                            | Verdict                         | Evidence                                                                                                            |
| --------------------------------------------------------------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| C1 Environment renders an Outposts section                      | **FAILED** (APP-7 confirmed)    | `C1-env-enterprise-rollout-tabs.png`, `C1-env-tab-outposts-deeplink-fallback.png`                                   |
| C2 Browser Back after "Back to app" returns to settings         | **FAILED** 3/3 (APP-6)          | `C2-back-after-back-to-app-attempt1/3-stuck-org-selector.png`                                                       |
| C3 Sub-org settings cold load without console errors            | **FAILED** (APP-1)              | `C3-suborg-cold-401s.log`, `C3-suborg-cold-load-renders.png`                                                        |
| C4 Session tools panel exposes `tab`/`tabpanel` roles           | **FAILED** (APP-13, DOM-proven) | `C4-a11y.txt`, `C4-session-tools-progress-open.png`                                                                 |
| C5 Manage-permissions drawer lists repositories                 | PASSED (ENT-26 not reproduced)  | `C5-repos-drawer-loaded-picker.png`                                                                                 |
| C5 Repository picker search with no match shows an empty state  | **FAILED** (ENT-28, new)        | `C5-repos-drawer-navflow-search-no-empty-state.png`                                                                 |
| C6 Personal Connections renders provider rows                   | **FAILED** (INFRA-5 confirmed)  | `C6-personal-connections-no-slack-no-github-rows.png`, `C6-ent-connections-slack-notconnected-github-notlinked.png` |
| C7 `/settings/usage` resolves to a working page                 | PASSED (ENT-22 not reproduced)  | `C7-usage-redirects-to-usage-policies.png`                                                                          |
| C8 Blueprint editor caret/keyboard nav + Discard restores       | PASSED                          | `C8-blueprint-caret-keyboard.png`, `C8-blueprint-discard-restored.png`                                              |
| C9 Invite dialog rejects invalid e-mail addresses               | **FAILED** (ENT-29, new)        | `C9-invite-invalid-email-add-enabled.png`, `C9-invite-script-payload-add-enabled.png`                               |
| C9 Script payload renders inert; Escape closes without mutating | PASSED (62 members unchanged)   | `C9-invite-escape-closed-62-members.png`                                                                            |
| C10 Sub-org quota banner (root cause for APP-5 / E2E stalls)    | PASSED (observation)            | `C10-suborg-usage-limit-banner-19-sessions.png`                                                                     |
| C11 Knowledge / Playbooks / Sessions / Members no-match states  | PASSED                          | `C11-*-no-match-empty-state.png`, `C11-members-hostile-input-empty-state.png`                                       |
| C11 Analytics extreme past range (2019-01-01 → 2019-01-02)      | PASSED (nit: label omits year)  | `C11-analytics-extreme-past-range-2019.png`, `C11-analytics-baseline-this-month.png`                                |
| C12 Usage Policies / Learning / Devin Coach render + deep-link  | PASSED                          | `C12-usage-policies-hub.png`, `C12-learning.png`, `C12-devin-coach.png`, `C12-usage-tiers.png`                      |
| C13 Network inventory: zero 5xx across the sweep                | PASSED with caveats             | `C13-network-summary.log`, `network.log`                                                                            |
| C13 No malformed request URLs                                   | **FAILED** (ENT-30, new)        | `C13-network-summary.log`, `network.log` lines 3/5/7/9                                                              |

## Findings

**ENT-28 (new) — Repository permissions picker has no no-results state**
Repro: `/org/cog-enterprise-qa/settings/repositories` → select `jeet-devin-qa` → `Manage
permissions` → type `nav-flow` in the picker search.
Expected: an explicit "no repositories match" state. Actual: a blank region, indistinguishable from
a stalled load. Evidence: `C5-repos-drawer-navflow-search-no-empty-state.png`.

**ENT-29 (new) — Invite members accepts syntactically invalid addresses**
Repro: Membership → `Invite members` → enter `not-an-email`, then
`"><script>alert(1)</script>@x.com`.
Expected: inline validation error and a disabled `Add`. Actual: `Add` stays enabled with no error
for both inputs. The payload renders as escaped literal text (no execution) and Escape closes the
dialog without mutating (member count stayed 62), so the defect is validation only. `a@b` was not
exercised. Evidence: `C9-invite-invalid-email-add-enabled.png`,
`C9-invite-script-payload-add-enabled.png`.

**ENT-30 (new, needs one confirmation run) — malformed request host during auth/org-selector**
4× `404 GET https://cog-enterprise-qa.beta.devinenterpri` + `cog-enterprise-qa.beta.
devinenterprise.com/api/auth1/auth-provider` (truncated host with the full host concatenated onto
it, and path `auth1` rather than `auth`), logged verbatim from CDP `response.url()` at
23:20:20–23:20:27Z; no manual navigation used such a URL. Expected: no malformed request URLs.
Evidence: `C13-network-summary.log`, `network.log` lines 3/5/7/9.

**APP-13 — confirmed at the DOM level.** The Progress panel opens visibly, but the document has 0
`role="tab"` and 0 `role="tabpanel"` elements; the tab is a plain
`<button role="button" aria-selected="true">` with no `aria-controls` inside a single
`role="tablist"`. Evidence: `C4-a11y.txt`.

**APP-1 — quantified.** A sub-org cold load emits exactly two distinct 401s
(`GET /api/promo/codes/list`, `POST /api/users/set-attachment-cookie`); the suite's "4× 401" is
2 endpoints × 2 page loads. Plus 7 cosmetic `img.clerk.com` avatar 404s. Zero 5xx anywhere.

**APP-6 — deterministic this sweep** (3/3 stuck on `/org-selector`), unlike the 1/3 Playwright probe.

**APP-5 root cause revised (infrastructure, not scheduler).** `/org/jeet-devin-qa` renders "19 of
your sessions were stopped by a usage limit that has since been raised." with a `Review & restart`
button, ~10 Recent sessions labelled "Exceeded limit" and 3 "Crashed". So `AUTO-E2E01`,
`SUB-AU-E2E01`, `SCHED-E2E01`, `GUARD-REG04`, `ECON-E2E01`, `KNOW-E2E01`, `PLAY-E2E01` are
quota-blocked, not product defects. Evidence: `C10-suborg-usage-limit-banner-19-sessions.png`.

**ENT-22 and ENT-26 did not reproduce** (`/settings/usage` resolves to a working Usage Policies hub;
the repository picker loads within 30 s).

Minor UX nit: the Analytics custom-range button reads "Custom (Jan 1 - Jan 2)" with no year, so a
2019 range is indistinguishable from 2026.

## Explicitly not exercised (do not read as passing)

- All non-admin/member authorization halves, including `MBR-REG01..07` (INFRA-1, no member
  credential).
- All OTP flows: `AUTH-SAN04`, `AUTH-REG01/REG02/REG05`, `MEMB-E2E01` (INFRA-2 — deliberately not
  retried to avoid burning the rate-limited inbox).
- `REV-E2E01` GitLab enrolment (INFRA-3).
- Every session-spawning outcome (`GUARD-REG04`, `ECON-E2E01`, `KNOW-E2E01`, `PLAY-E2E01`,
  `AUTO-E2E01`, `SUB-AU-E2E01`, `SCHED-E2E01`) — quota-blocked, see above.
- Every mutation-persistence assertion (non-destructive sweep: no Save was ever clicked).
- Invite `a@b` and the 12-Tab focus-trap sequence of the invite dialog.
- The ">5 s request" and ">10 polls/min" halves of `CH-ENTEXP-006` (the logger recorded status ≥400
  only).

## Environment requests raised by the sweep

- A non-admin member credential on `cog-enterprise-qa` (INFRA-1).
- Relief on the Gmail OTP rate limit or a dedicated QA inbox (INFRA-2).
- A raised/reset session quota on `jeet-devin-qa` so the session-spawning E2E cases can run.
