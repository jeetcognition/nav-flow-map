# 23 — Secrets (sub-organization)

Page: `/org/jeet-devin-qa/settings/secrets`.

| ID | Type | Pri | How to reach | Steps | Expected |
|---|---|---|---|---|---|
| SECRET-SMK01 | Smoke | P1 | jeet-test-org → Settings → Secrets | Load the page cold. | Breadcrumb and back navigation, help text, Organization and Personal counts, search, Bulk add secrets, Add secret, table, and empty or list state render without page or console errors. |
| SECRET-SAN01 | Sanity | P1 | Secrets | Inspect both scopes. | Organization and Personal tabs show independent counts; Name, Type, Note, Updated by, and Updated at columns are readable without displaying secret values. |
| SECRET-SAN02 | Sanity | P1 | Secrets → empty scope | Inspect the empty state. | No secrets found and Add your first secret appear; controls remain usable and no row from the other scope leaks into the view. |
| SECRET-SAN03 | Sanity | P1 | Secrets → Add secret | Open the dialog without submitting. | Scope, type, name, value, note, Redact value, close, and Store secret controls appear; the title reflects the selected scope. |
| SECRET-SAN04 | Sanity | P1 | Secrets → Add secret / Bulk add secrets | Inspect Raw secret, Cookie, TOTP, and bulk-import forms. | Raw, Cookie JSON or base64, TOTP and QR, upload-file, pasted `.env`, scope, and gated Store controls match the selected workflow. |
| SECRET-REG01 | Regression | P1 | Secrets | Search each scope using full or partial name, no-match, whitespace, case variants, Unicode, long, HTML-like, and injection-like input; clear it. | Filtering is literal and safe; no-match is clear; clearing restores only the active scope's rows. |
| SECRET-REG02 | Regression | P1 | Secrets → Add secret | Test blank, whitespace, punctuation, leading-digit, very long, Unicode, HTML-like, and duplicate names. | Required validation is clear; unsafe text remains inert; documented environment-variable normalization and duplicate suffixing are deterministic and collision-safe. |
| SECRET-REG03 | Regression | P1 | Secrets → Add secret → Raw secret | With approval, create disposable Raw secrets with blank, whitespace, multiline, Unicode, very long, and special-character values plus long or HTML-like notes. | Required value validation is actionable; supported values persist exactly; notes render inertly; plaintext is never exposed after saving. |
| SECRET-REG04 | Regression | P1 | Secrets → Add secret → Cookie | Test valid JSON or base64 and malformed JSON, invalid base64, missing fields, unsupported domains or paths, duplicates, and oversized payloads. | Only valid Chromium-format cookies are accepted; errors identify the problem without logging cookie contents. |
| SECRET-REG05 | Regression | P1 | Secrets → Add secret → One-Time Password (TOTP) | Test valid disposable `otpauth://` data, malformed URI or base32, missing issuer, account, or secret, oversized input, and denied QR-camera access. | Valid dedicated-account TOTP data is accepted; invalid data and scanner failures are handled safely without exposing the seed. |
| SECRET-REG06 | Regression | P1 | Secrets → Bulk add secrets | Paste or upload `.env` data containing comments, blank lines, quoted or multiline values, `=` inside values, duplicate keys, invalid lines, Unicode, and mixed-validity rows. | Parsing is consistent between paste and file upload; preview or errors identify affected keys without exposing values; no partial import occurs unless explicitly confirmed. |
| SECRET-REG07 | Regression | P1 | Secrets → Add or Bulk add | Switch Organization and Personal scopes and secret types after entering unsaved data; close, press Escape, go Back, refresh, and revisit. | Scope, title, and fields remain consistent; warning or documented discard behavior occurs; no secret is silently stored or moved between scopes. |
| SECRET-REG08 | Regression | P1 | Secrets → disposable secret | With approval, create a disposable secret → verify metadata → edit name, value, note, and redaction → reload → delete it. | CRUD persists correctly; editing never reveals the previous value unnecessarily; only the selected secret changes; cleanup removes it. |
| SECRET-REG09 | Regression | P1 | Secrets → disposable secret → Actions → Delete | With approval, cancel once, then confirm; repeat with similarly named rows. | Confirmation identifies the exact secret and scope; cancel changes nothing; confirm removes only the selected secret and leaves no usable residual value. |
| SECRET-REG10 | Regression | P0 | Secrets | With approval, create disposable Personal and Organization secrets, then inspect as creator, another member, admin, and non-member. | Personal secrets are visible and usable only in the creator's sessions; Organization secrets are usable by organization members, while view and edit privileges follow documented admin rules. |
| SECRET-REG11 | Regression | P1 | Secrets and existing sessions | With approval, add, update, and delete a disposable secret while old and new sessions exist. | Changes follow documented session timing: new secrets become available to newly created sessions, and stale sessions do not gain unexpected access. |
| SECRET-REG12 | Regression | P0 | Secrets → Add secret → Redact value | With approval, enable redaction, use a disposable marker in a new session, and inspect the UI, DOM, console, requests, transcript, logs, and errors. | The value remains usable where intended but is masked from user-visible and diagnostic surfaces; copying, editing, and failures do not reveal it. |
| SECRET-REG13 | Regression | P0 | Secrets | As a non-admin or with tampered organization or secret IDs, attempt list, create, view, edit, delete, bulk import, and scope-changing requests. | Server-side authorization prevents IDOR, privilege escalation, cross-organization access, and access to another user's Personal secrets. |
| SECRET-E2E01 | E2E | P0 | Secrets and new sessions | With approval, create unique disposable Personal and Organization Raw secrets → start new creator and member sessions that reference `$SECRET_NAME` → verify scope and value use without printing sensitive data → edit and retest in new sessions → delete both. | Correct secrets are injected only into allowed new sessions; Personal and Organization isolation and updates work; cleanup confirms later sessions cannot access either disposable secret. |
